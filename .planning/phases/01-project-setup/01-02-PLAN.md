---
wave: 2
depends_on: [01-01]
files_modified:
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/lib/utils.ts
  - tailwind.config.js
  - postcss.config.js
  - src/index.css
autonomous: true
requirements: []
---

# Plan 01-02: Configure shadcn/ui

## Objective

Set up shadcn/ui component library with Tailwind CSS and install essential components for testing.

## Context

From CONTEXT.md:
- Use shadcn/ui for components (locked decision)
- Professional design from the start
- Claude's discretion on which components to install upfront vs on-demand
- Claude's discretion on theme configuration

From PROJECT.md:
- Design should be professional and responsive
- Professional appearance is a key constraint

## Tasks

<task id="1">
Install Tailwind CSS and its dependencies:
- Install tailwindcss, postcss, autoprefixer
- Run `npx tailwindcss init -p` to create config files
</task>

<task id="2">
Configure Tailwind CSS:
- Update tailwind.config.js with content paths for React components
- Set up color scheme for professional appearance (use neutral/slate base)
- Configure font families if needed
</task>

<task id="3">
Update src/index.css with Tailwind directives and base styles:
- Add @tailwind directives for base, components, utilities
- Add any custom CSS variables or base styles
</task>

<task id="4">
Initialize shadcn/ui in the project:
- Run `npx shadcn-ui@latest init`
- Configure for TypeScript, React, and Tailwind
- Set up components.json with proper paths
</task>

<task id="5">
Install essential shadcn/ui components for testing:
- Install Button component (`npx shadcn-ui@latest add button`)
- Install Card component (`npx shadcn-ui@latest add card`)
- These will validate the setup and be useful for Phase 2 form
</task>

<task id="6">
Verify shadcn/ui setup:
- Import and render a Button and Card in App.tsx
- Check that Tailwind classes apply correctly
- Confirm components have proper styling and professional appearance
</task>

<task id="7">
Clean up after verification:
- Remove test components from App.tsx
- Leave App.tsx ready for Phase 2 to add the actual form
</task>

## Verification Criteria

- [ ] Tailwind CSS is installed and configured
- [ ] shadcn/ui CLI works and components.json is created
- [ ] Button component renders with correct styling
- [ ] Card component renders with correct styling
- [ ] Tailwind utility classes work in components
- [ ] No console errors or warnings
- [ ] Professional appearance (clean, modern design)

## Must Haves

From Phase 1 goal: "shadcn components render correctly"

- shadcn/ui is properly configured
- Tailwind CSS integration works
- Test components (Button, Card) render with correct styles
- Components have professional, modern appearance
- Setup is ready for Phase 2 to build the form
