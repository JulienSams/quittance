---
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/accordion.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/lib/utils.ts
autonomous: true
---

# Plan 02-02: Add shadcn Form Components

## Objective

Install and configure shadcn/ui form components needed for the accordion-based form (Accordion, Input, Label).

## Context

From CONTEXT.md:
- Form uses accordion/collapsible sections
- Need to add Accordion component from shadcn/ui (not yet installed)
- Will also need Input and Label components for form fields

## Tasks

<task id="install-accordion">
Install shadcn/ui Accordion component:

```bash
npx shadcn@latest add accordion
```

Verify the component is created at `src/components/ui/accordion.tsx`.
</task>

<task id="install-input">
Install shadcn/ui Input component:

```bash
npx shadcn@latest add input
```

Verify the component is created at `src/components/ui/input.tsx`.
</task>

<task id="install-label">
Install shadcn/ui Label component:

```bash
npx shadcn@latest add label
```

Verify the component is created at `src/components/ui/label.tsx`.
</task>

<task id="verify-components">
Test that components render correctly:
- Create a temporary test component that uses Accordion, AccordionItem, AccordionTrigger, AccordionContent
- Verify Accordion opens/closes properly
- Verify Input accepts text input
- Verify Label displays correctly with Input

Clean up test component after verification.
</task>

## Verification Criteria

- [ ] Accordion component installed and renders
- [ ] Input component installed and accepts input
- [ ] Label component installed and displays
- [ ] All components use existing Tailwind theme
- [ ] No console errors or warnings
- [ ] Components work on mobile viewport

## Must-Haves

**From Phase Goal:**
- Form UI with shadcn components ✓
